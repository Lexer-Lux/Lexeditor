[pytest]
testpaths = tests
# No .pytest_cache folder: last-failed/step-wise state is not worth a
# directory of litter in the checkout.
addopts = -p no:cacheprovider
norecursedirs = .git .venv out build dist
