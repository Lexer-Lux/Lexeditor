# RDR fonts

The RDR plugin uses the same official Rockstar webfonts as the RDR2 skin. A
private font already installed for the RDR2 plugin can satisfy the dependency.
Font binaries are not project source.
