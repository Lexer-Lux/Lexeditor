# Worklogs

Current attempts are owned by one file per GitHub issue under `issues/`. Pre-GitHub TODO history and mixed/session notes are preserved under `legacy/`. `../Worklog.txt` is a generated compatibility index; worktree agents never edit it directly.

## Migrated source preamble

# GameplayTweaks worklog

Agent-facing implementation history, runtime evidence, failed approaches, and validation notes.
GitHub issues are the live what/why/status tracker. `TODO.txt` is deprecated archival material. `../CODEX.txt` is the generated technical reference index.
