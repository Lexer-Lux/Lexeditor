## Current scope
Runtime candidates for Lexer-Lux/Lexeditor#181, #357, #120 and #194. This is partial RDR2 backlog work, not a claim that all issues or the reported binocular crash are resolved. Central issue handoffs remain in Lexeditor; no issue is automatically closed.

## Changes
- **Binoculars:** retire the explicitly permitted ineffective TransitionAnimRate control and setter calls. Preserve native draw/stow, early-release latch, readiness and timing. Guard all three prompt-registry pointer reads and add specific crash-stage markers. This removes a demonstrated null-dereference hazard, not a proven cause of #357.
- **Development mode/builds:** initialize the shared readout latch from the build mode; preserve the existing Tilde toggle. Repair the fresh-checkout build entrypoint, VS discovery/quoting and staged compiler outputs. Existing stale binaries cannot be silently blessed.
- **Radial ammo:** recover the two recorded widgets routes, restore the Size/visibility binding, disable the X/Y RawText binding instead, restore baseline 7170, and fix negative NudgeX/NudgeY values becoming large unsigned coordinates. Preserve wheel input, family order and the existing numeric font. Report effective alignment in the existing heartbeat.
- Add exact, read-only package verification and executed production-renderer regression tests. Windows artifacts contain the matching ASI plus lml/RadialAmmoCounts resources and installation/rollback notes, without replacing the user's INI or redistributing the SDK/fonts.

## Verified
Both complete Windows release and development builds passed for code revision `ca2cfb6229e06f4cfd316614d95f5b84077cde90` (PR merge revision `c2229994966557ed08320b82c65949cfa3522064`): Windows run 34044885479. Both artifacts were downloaded; each ASI matched its manifest hash, and both packaged radial resources matched the reviewed bytes/routes.

Six production-renderer execution cases and seven package-rejection tests passed, alongside the existing binocular, development-latch, settings-schema and casing-preservation checks: source run 34044885506. The pre-repair renderer failed the new placement and signed-offset cases. Six Windows build-entrypoint cases also passed. The generated menu remained reproducible.

## Acceptance still open
No installation, game launch or gameplay/Windows UI acceptance occurred. Radial routes were recovered from recorded extraction evidence, not freshly extracted this session. Actual VFS binding, the visible absence of old X/Y, input compatibility and binocular crash reproduction still require testing. The overlay dims zero-stock text, not native icons; icon dimming remains unimplemented. Keep broader issues open/actionable.

The superseded initial PR report is preserved verbatim at worklog/attachments/rdr2-issue-batch/pr211-before-radial-summary.md. Its old 'no production ASI build' statement no longer describes the current candidate. Temporary import workflows were removed. Master and other games were not edited.