# RDR1 sweep — 2026-09-06

All 17 open rdr1 issue bodies reviewed. No issue is closed by this pass. Changes are in PR #362.

## Implemented slice

#331/#337: editor safety and truthful workspace destinations improved; complete coverage and actual delivery remain.
#341: decimal shop prices now use float32 readback/no-op semantics; full unpacked-resource verification occurs before replacing the override or backup. A failed/corrupt repack preserves existing files.
#344: strict mission IDs/schema/rewards and blank-input rejection; save/reset tests preserve the read-only generated table. Actual reward delivery remains unverified.
#19: existing split UI retained; six rendered screenshots and browser interactions checked at 1600x900 and 1280x720. Installed WebView2/player review is distinct.
#55: synthetic RDR cache checks pass; installed startup and FF8 checks were not performed. No other game's source changed.

## Unfinished native work

#333 crash history reviewed; logs do not identify the faulting instruction and no native repair is claimed.
#334 horse marker/icons, #335 shop quantity wheel, #336 camera bindings, #338 carriage rest, #342 cover shoulder swap, #343 first-person V and #345 Skip/Travel require native implementation/reference source absent from this checkout. None is implemented by this patch.
#332 dev mode, #339 startup logos and #340 money/ammo HUD describe existing installed candidates; no new build or human game test was performed.
Shop/mission end-to-end deployment, rollback and reproducible in-game test setup also remain unfinished.

The configured runtime repository redirects to Lexer-Lux/Lexers-Mod-For-RDR, whose GitHub contents endpoint reports an empty repository. C:\RDRMod is the configured external runtime root. Library search did not identify a usable source archive. Missing source does not establish unfeasibility. Remaining agent implementation/delivery stays actionable; source/PR/CI success is not an installed test candidate.

## Verification

[CI run 34035835307](https://github.com/Lexer-Lux/Lexeditor/actions/runs/34035835307) passed 18 unit tests with invalid-input/corruption subcases, existing RDR cache/layout contracts, and actual Chromium browser checks. The unit suite fails against the original source. All six CI screenshots were downloaded and inspected: complete list rows, independent detail panes and no horizontal page overflow. Browser checks cover multi-tab preflight, 1.1 shop save/readback, fractional-loot rejection, loot discard, missing optional evidence and malformed-loot recovery without JavaScript page errors.

Fixtures and the identity resource codec are synthetic, not installed game assets or a real RSC85 compressor. No native build, game launch, installed WebView2 acceptance, or in-game result is claimed. Local browser navigation was administratively blocked; successful rendered testing used the standard GitHub-hosted CI runner.
