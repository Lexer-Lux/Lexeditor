# #507

Live issue: https://github.com/Lexer-Lux/Lexeditor/issues/507


## Folded in: PR #492 handoff

## Factorio PR #492 handoff

Scope is Factorio only. Branch `codex/factorio-plugin`; PR title `Factorio Plugin`; keep the PR open/draft. Never merge or push master, enable auto-merge, delete the branch, or overwrite an existing Lexeditor/game installation.

### Current implementation

Reconciled with current master `95a15cae8f0e53f2884222f573f8222600aaae58`; PR is draft/open and must not be merged by this worker.

Scope isolation completed 2026-09-22: the live PR branch was rebuilt from current master with only Factorio files plus required shared registration/Credits/mod-loading metadata. Pre-isolation head `e1fac3a1c54bf159c6b8ef791da78989cce8d0ae` is preserved at `backup/pr-492-pre-isolation-e1fac3a`. No FF8/RDR2/Todo/distribution changes remain in the PR diff.


- Immutable Factorio `--dump-data` JSON import; arbitrary Lua is rejected rather than evaluated.
- Structured recipes/items/machines/technologies editors using shared paged Table+Detail, with current-master relative module routing and plugin-owned CSS only.
- Real prototype identity, modeled range/reference validation, source-graph diagnostics.
- Atomic separate `overrides.json`; Save/Discard/Reopen.
- Native Factorio 2.1 override-mod export to project `build/`, with deterministic ZIP bytes.
- Generated `data-final-fixes.lua` contains only validated assignments and guards missing prototype identities.
- Authored dependencies are preserved; enabled source mods become optional load-order dependencies except base and the generated mod itself.
- Version 2.1.x support plus separate installed/active Space Age, Quality, Elevated Rails reporting; configured non-2.1 installs and invalid project manifests fail closed for edit/save/export.
- Truthful Data Map for partial and unsupported prototype families.
- Synthetic model fixture and real-service Playwright edit/save/reopen/export harness covering recipe, item, assembling-machine and technology edits, technology reference editing, paging, empty state, discard, screenshots and stale-source export rejection.
- Factorio-scoped CI uploads rendered screenshots, results, and isolated candidate.

### Evidence paths

- Source/model tests: `tests/test_factorio.py`
- Rendered interaction test: `tests/factorio_browser_check.py`
- Synthetic source: `tests/fixtures/factorio/`
- CI workflow: `.github/workflows/factorio-checks.yml`
- CI artifact: `factorio-rendered-candidate`
- Research record: `codex/factorio/README.md`

### Acceptance boundary

CI can prove parsing, validation, roundtrip, deterministic package construction, source immutability, the actual loopback service, and rendered UI behavior. It cannot prove that Factorio itself accepts the generated mod without launching the proprietary game. Do not call real-game loading verified until that exact candidate has been loaded by Factorio.

Human in-game acceptance after agent checks:
1. Use the same Factorio 2.1 installation and source-mod set used to create the dump.
2. Put the isolated generated ZIP in that Factorio profile's mods directory and enable it through Factorio's normal mod UI.
3. Launch and confirm there are no prototype-stage errors in the Factorio log.
4. Verify edited recipe time/enabled/productivity, item stack size, machine crafting speed, and technology prerequisites/count/time in the relevant Factorio UI/Factoriopedia or gameplay.
5. Confirm the original source mods and imported JSON dump remain unchanged.
6. Remove/disable the generated mod to return to the previous state.

Agent work remains actionable until the exact-head Factorio source jobs pass, the rendered job passes with inspected screenshots/results/candidate, shared current-master contracts are checked, and the PR checklist records the evidence. Real-game acceptance remains separate.
