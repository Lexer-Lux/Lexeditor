# #483 — Timed Blocks

## Requirement
A well-timed press as an enemy physical hit lands reduces that hit's damage
(proposal ×0.5). A visible cue shared with Universal Timed Hits. Success and
failure sounds, each chosen with a sound selector in the tweak's options
(Lexer, 2026-09-26). Off by default, and off is byte-identical to vanilla.

## Research (2026-09-26; see codex/ff8/timed-hits.md)
- Damage is applied at command resolution: `00494410` writes the target's
  new HP (`+0x18`, `004946BC`) before the enemy animation plays. The
  animation only displays the number later.
- So a block cannot reduce damage at the source. It has to refund half the
  damage at the hit frame and correct the displayed number.
- Input: R1 is bit `0x08`. Whether the ATB menu also consumes it during an
  enemy turn is still open.

## Decision (Lexer, 2026-09-26)
Defer the HP write to the hit frame rather than refunding afterwards. The
block halves the damage when the hit lands. Everything that reads target HP
between resolution and the hit frame must be audited: KO checks, counters,
enemy AI reactions, crisis level, and the win/lose check.

## Next agent work
- Identify the hit-frame opcode, the moment damage shows. `0x91` and `0x9D`
  are the only opcodes that call into battle logic; neither is proved yet.
- Then design the refund and the cue, and implement it off by default.

## Hit-frame diagnostic (2026-09-26, branch claude/ff8-exe-patches)
- Tweaks -> HIT-FRAME LOG (`hitFrameLog`, off by default) adds a Hext cave at
  `027A9000` hooked at the opcode handler `00504BB0`. It appends
  `<tsc> <owner> <instruction address> <opcode>` for every animation
  instruction to `lexeditor-hitframe.log` in the game folder.
- `tests/ff8/test_ff8_hit_frame_log.py` runs the cave under unicorn with stubbed
  kernel32 imports. The hook bytes are verified against the pinned exe.
- Waiting on Lexer: one or two battles with the log on, then send the log.
- `tests/ff8/verify_ff8_gameplay_quarantine_issue_21.py` was already failing
  on master (it reads editor.html, but tweaks are built in boot.js). It is
  not in active CI and was left alone.
