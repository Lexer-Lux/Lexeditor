-r requirements.txt
pytest>=8,<10
playwright>=1.50,<2
# The browser verifiers speak CDP over a websocket (tools/cdp.py).
websocket-client>=1.7,<2
# FF8 native-code verifiers assemble and emulate x86 hooks.
keystone-engine==0.9.2
unicorn==2.1.4
