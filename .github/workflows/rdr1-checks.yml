name: RDR1 checks
on:
  workflow_dispatch:
  push:
    branches: [master, fix/rdr1-editor-runtime-handoff]
    paths:
      - 'plugins/rdr/**'
      - 'tools/*rdr*.py'
      - 'tests/test_rdr_*.py'
      - 'tests/rdr_browser_check.py'
      - '.github/workflows/rdr1-checks.yml'
  pull_request:
    paths:
      - 'plugins/rdr/**'
      - 'tools/*rdr*.py'
      - 'tests/test_rdr_*.py'
      - 'tests/rdr_browser_check.py'
      - '.github/workflows/rdr1-checks.yml'
permissions:
  contents: read
jobs:
  editor:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out source without initializing optional gitlinks
        run: |
          git init .
          git -c protocol.version=2 fetch --depth=1 "https://github.com/${GITHUB_REPOSITORY}.git" "$GITHUB_SHA"
          git checkout --detach FETCH_HEAD
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Unit and existing cache/layout contracts
        shell: bash
        run: |
          mkdir -p artifacts
          python -m compileall -q plugins/rdr
          python -m unittest discover -s tests -p 'test_rdr_string_tables.py' 2>&1 | tee artifacts/rdr1-strtbl.log
          python -m unittest discover -s tests -p 'test_rdr_rbf.py' 2>&1 | tee artifacts/rdr1-rbf.log
          python tools/verify_rdr_editing.py 2>&1 | tee artifacts/rdr1-unit.log
          python tools/verify_rdr_cache_reuse_71.py | tee artifacts/rdr1-cache.log
          python tools/verify_rdr_items_split_issue_19.py | tee artifacts/rdr1-split.log
          python tools/verify_rdr_data_map_audit.py | tee artifacts/rdr1-data-map-audit.log
          python tools/verify_rdr_mod_compatibility.py | tee artifacts/rdr1-mod-compat.log
      - name: Install headless browser test dependency
        run: |
          python -m pip install playwright==1.57.0
          python -m playwright install --with-deps chromium
      - name: Browser interactions and complete-row checks
        shell: bash
        run: |
          python tools/verify_rdr_editing_browser.py --screenshots artifacts/screenshots 2>&1 | tee artifacts/rdr1-browser.log
          python tests/rdr_browser_check.py --screenshots artifacts/rdr-browser 2>&1 | tee artifacts/rdr1-shared-detail.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rdr1-editor-checks
          path: artifacts/
          retention-days: 7
