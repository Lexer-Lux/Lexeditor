name: Export FF8 camera package source
on:
  push:
    branches: [fix/ff8-world-camera-pitch]
    paths: [.github/workflows/ff8-camera-package-source.yml]
permissions:
  contents: read
jobs:
  export:
    runs-on: ubuntu-24.04
    steps:
      - name: Fetch exact revision without optional gitlinks
        run: |
          git init editor
          git -C editor remote add origin https://github.com/Lexer-Lux/Lexeditor.git
          git -C editor fetch --depth=1 origin "$GITHUB_SHA"
          git -C editor checkout --detach FETCH_HEAD
      - name: Archive tracked working tree
        run: |
          python - <<'PY'
          from pathlib import Path
          import os, subprocess, zipfile
          root=Path('editor')
          names=subprocess.check_output(['git','-C',str(root),'ls-files','-z']).decode().split('\0')
          with zipfile.ZipFile('ff8-camera-package-source.zip','w',zipfile.ZIP_DEFLATED) as z:
              z.writestr('SNAPSHOT_REVISION.txt', os.environ['GITHUB_SHA'])
              for name in names:
                  if not name: continue
                  p=root/name
                  if p.is_file(): z.write(p,name)
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: ff8-camera-package-source
          path: ff8-camera-package-source.zip
          retention-days: 2
          if-no-files-found: error
