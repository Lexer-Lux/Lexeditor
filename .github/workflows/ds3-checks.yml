# Generated by `python tools/check_plugin.py --write-workflows`; do not edit.
name: ds3 checks
on:
  push:
    branches: [master]
    paths:
      - 'plugins/ds3/**'
      - 'tests/ds3/**'
      - 'tests/plugin_checks.json'
      - 'tools/check_plugin.py'
      - 'tests/shared/verify_all.py'
      - 'requirements-test.txt'
      - '.github/workflows/ds3-checks.yml'
  pull_request:
    paths:
      - 'plugins/ds3/**'
      - 'tests/ds3/**'
      - 'tests/plugin_checks.json'
      - 'tools/check_plugin.py'
      - 'tests/shared/verify_all.py'
      - 'requirements-test.txt'
      - '.github/workflows/ds3-checks.yml'
  workflow_dispatch:
jobs:
  checks:
    runs-on: windows-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: python -m pip install -r requirements-test.txt
      - run: python -m playwright install chromium
      - run: python tools/check_plugin.py ds3
