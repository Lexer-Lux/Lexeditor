name: Temporary Bannerlord final hardening v2

on:
  push:
    branches: [feature/bannerlord-plugin]
    paths:
      - '.github/workflows/temp-bannerlord-final-hardening-v2.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/bannerlord-plugin
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply final hardening patch
        run: python tools/temp_bannerlord_final_hardening_v2.py
      - name: Focused validation
        run: |
          python -m py_compile games/bannerlord/source_revision.py games/bannerlord/gauntlet_data.py games/bannerlord/module_xml_data.py games/bannerlord/module_data.py games/bannerlord/project_data.py games/bannerlord/skill_data.py games/bannerlord/perk_data.py games/bannerlord/settings_data.py games/bannerlord/server.py tests/test_bannerlord_source_safety.py
          python -m unittest tests.test_bannerlord_source_revision tests.test_bannerlord_source_safety tests.test_bannerlord_gauntlet tests.test_bannerlord_moduledata tests.test_bannerlord_xsd tests.test_bannerlord_plugin tests.test_bannerlord_perks_runtime tests.test_bannerlord_settings tests.test_bannerlord_ui_contract -v
          python -c "from games.bannerlord.plugin import smoke; print(smoke())"
          python tools/verify_shared_ui_contract.py
          python -c "import glob, subprocess; [subprocess.run(['node','--check',path], check=True) for path in glob.glob('games/bannerlord/editor*.js')]"
          git diff --check
      - name: Commit final hardening
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add games/bannerlord/source_revision.py games/bannerlord/gauntlet_data.py games/bannerlord/module_xml_data.py games/bannerlord/module_data.py games/bannerlord/project_data.py games/bannerlord/skill_data.py games/bannerlord/perk_data.py games/bannerlord/settings_data.py games/bannerlord/server.py games/bannerlord/editor_boot.js games/bannerlord/editor_gauntlet.js games/bannerlord/editor_moduledata.js tests/test_bannerlord_source_revision.py tests/test_bannerlord_source_safety.py tests/test_bannerlord_gauntlet.py tests/test_bannerlord_moduledata.py tests/test_bannerlord_plugin.py tests/test_bannerlord_perks_runtime.py tests/test_bannerlord_settings.py
          git commit -m "Harden Bannerlord structured source writes"
          git push origin HEAD:feature/bannerlord-plugin

  regression:
    needs: patch
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 18
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/bannerlord-plugin
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: python -m pip install Pillow 'fonttools>=4.59,<5'
      - run: python -m compileall -q games/bannerlord desktop_host.py service_session.py plugin_api.py project_manager.py runtime_bootstrap.py
      - run: python -m unittest discover -s tests -p 'test_bannerlord*.py' -v
      - run: python -m unittest discover -s tests -p 'test_project*.py' -v
      - run: python -m unittest discover -s tests -p test_data_map_coverage.py -v
      - run: python -c "import glob, subprocess; [subprocess.run(['node','--check',path], check=True) for path in glob.glob('games/bannerlord/editor*.js')]"

  shared-contract:
    needs: patch
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/bannerlord-plugin
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python tools/verify_shared_ui_contract.py
      - run: python -m unittest tests.test_bannerlord_ui_contract -v

  browser:
    needs: patch
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/bannerlord-plugin
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install playwright
      - run: python -m playwright install --with-deps chromium
      - run: python tests/bannerlord_browser_check.py out/bannerlord-final-hardening-v2

  cleanup:
    if: always()
    needs: [patch, regression, shared-contract, browser]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/bannerlord-plugin
      - name: Remove temporary v2 hardening harness
        run: |
          rm -f tools/temp_bannerlord_final_hardening_v2.py .github/workflows/temp-bannerlord-final-hardening-v2.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m "Remove temporary Bannerlord final hardening v2 harness"
            git push origin HEAD:feature/bannerlord-plugin
          fi
