# Generated by `python tools/check_plugin.py --write-workflows`; do not edit.
name: project_zomboid checks
on:
  push:
    branches: [master]
    paths:
      - 'plugins/project_zomboid/**'
      - 'tests/*project_zomboid*'
      - 'tests/plugin_checks.json'
      - 'tools/check_plugin.py'
      - 'tests/verify_all.py'
      - 'requirements-test.txt'
      - '.github/workflows/project_zomboid-checks.yml'
  pull_request:
    paths:
      - 'plugins/project_zomboid/**'
      - 'tests/*project_zomboid*'
      - 'tests/plugin_checks.json'
      - 'tools/check_plugin.py'
      - 'tests/verify_all.py'
      - 'requirements-test.txt'
      - '.github/workflows/project_zomboid-checks.yml'
  workflow_dispatch:
jobs:
  checks:
    runs-on: windows-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: python -m pip install -r requirements-test.txt
      - run: python -m playwright install chromium
      - run: python tools/check_plugin.py project_zomboid
