name: Targeted UI visual fixes

on:
  push:
    branches: [fix/ui-visual-acceptance-20260906]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/ui-visual-acceptance-20260906
          fetch-depth: 0
      - name: Apply targeted visual fixes
        run: |
          python .github/scripts/ui_graph_patch.py
          python .github/scripts/ui_blank_graph_patch.py
          python .github/scripts/ui_acceptance_relative_patch.py
      - name: Validate fixture
        run: |
          python -m py_compile .github/scripts/ui_graph_patch.py .github/scripts/ui_blank_graph_patch.py .github/scripts/ui_acceptance_relative_patch.py .github/scripts/ui_visual_acceptance.py
          node --check ui/framework.js
          ! grep -q 'function editableTablePanel' games/blank/editor.html
          ! grep -q 'blank-editable-table' games/blank/editor.html
      - name: Commit targeted fixes
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name 'Lexeditor UI maintenance'
          git config user.email '89482099+Lexer-Lux@users.noreply.github.com'
          git add ui/framework.css games/blank/editor.html .github/scripts/ui_visual_acceptance.py
          git commit -m 'Fix responsive graph presentation'
          git push origin HEAD:fix/ui-visual-acceptance-20260906
